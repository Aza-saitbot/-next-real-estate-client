import React from 'react';
import withAuth from "@/shared/hoc/withAuth";

const Admin = () => {
    return (
        <div>
            adminооооооооооо
        </div>
    );
};

export default withAuth(Admin);